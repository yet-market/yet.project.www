---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// Feature data
const features = [
  {
    icon: 'mcp',
    title: 'MCP Server',
    description: 'Native Model Context Protocol support with 27+ tools. Claude, Cursor, and Windsurf can manage your projects directly.',
    color: 'primary',
  },
  {
    icon: 'api',
    title: 'REST API',
    description: '68+ endpoints covering tasks, projects, comments, time logs, and more. Full programmatic access to everything.',
    color: 'secondary',
  },
  {
    icon: 'cli',
    title: 'CLI Tool',
    description: 'Manage projects from your terminal. Create tasks, update progress, log time - without leaving your editor.',
    color: 'accent',
  },
  {
    icon: 'knowledge',
    title: 'Knowledge Base',
    description: 'Persistent AI memory that survives between sessions. Store context, decisions, and learnings for smarter AI.',
    color: 'primary',
  },
  {
    icon: 'keys',
    title: 'API Keys',
    description: 'Granular permissions for AI agents. Control read, write, and admin access for each integration.',
    color: 'secondary',
  },
  {
    icon: 'activity',
    title: 'Activity Tracking',
    description: 'See exactly what AI did vs humans. Full audit trail for every action with agent identification.',
    color: 'accent',
  },
];

// Integration logos
const integrations = [
  { name: 'Claude Code', logo: 'claude' },
  { name: 'Cursor', logo: 'cursor' },
  { name: 'Windsurf', logo: 'windsurf' },
  { name: 'GitHub', logo: 'github' },
  { name: 'VS Code', logo: 'vscode' },
  { name: 'Claude Desktop', logo: 'claude-desktop' },
];

// How it works steps
const steps = [
  {
    step: '01',
    title: 'Create Your Workspace',
    description: 'Sign up and create a tenant in seconds. Set up your first project and invite your team.',
    code: `$ yet login --key yet_abc123...
✓ Authenticated as john@company.com
✓ Connected to workspace: Acme Corp`,
  },
  {
    step: '02',
    title: 'Configure MCP Server',
    description: 'Add the Yet.Project MCP server to Claude Code, Cursor, or any MCP-compatible tool.',
    code: `{
  "mcpServers": {
    "yet": {
      "command": "npx",
      "args": ["-y", "@anthropic/yet-mcp"],
      "env": { "YET_API_KEY": "yet_..." }
    }
  }
}`,
  },
  {
    step: '03',
    title: 'Let AI Manage Tasks',
    description: 'Your AI assistant can now create tasks, update progress, log time, and access project context.',
    code: `> "Claude, create a task for implementing auth"
✓ Created task: Implement user authentication
  ID: task_abc123
  Project: Backend API
  Priority: High`,
  },
];

// Pricing plans
const pricing = [
  {
    name: 'Free',
    price: '$0',
    period: 'forever',
    description: 'Perfect for individuals and hobby projects',
    features: [
      'Up to 3 projects',
      'Up to 100 tasks',
      '1 team member',
      'REST API access',
      'CLI tool',
      'Community support',
    ],
    cta: 'Get Started',
    href: 'https://app.yet.watch/auth/register',
    highlighted: false,
  },
  {
    name: 'Pro',
    price: '$12',
    period: '/user/month',
    description: 'For teams building with AI',
    features: [
      'Unlimited projects',
      'Unlimited tasks',
      'Unlimited members',
      'MCP Server access',
      'Knowledge Base',
      'API keys with permissions',
      'Priority support',
    ],
    cta: 'Start Free Trial',
    href: 'https://app.yet.watch/auth/register?plan=pro',
    highlighted: true,
  },
  {
    name: 'Enterprise',
    price: 'Custom',
    period: '',
    description: 'For organizations at scale',
    features: [
      'Everything in Pro',
      'SSO / SAML',
      'Audit logs',
      'Custom integrations',
      'Dedicated support',
      'SLA guarantee',
      'On-premise option',
    ],
    cta: 'Contact Sales',
    href: '/contact',
    highlighted: false,
  },
];

// Stats
const stats = [
  { value: '68+', label: 'API Endpoints' },
  { value: '27+', label: 'MCP Tools' },
  { value: '10k+', label: 'Developers' },
  { value: '99.9%', label: 'Uptime' },
];

// FAQ data
const faqs = [
  {
    question: 'What is Yet.Project and how is it different from Jira or Linear?',
    answer: 'Yet.Project is an AI-native project management platform built specifically for developers working with AI assistants. Unlike Jira or Linear, we provide native MCP (Model Context Protocol) support, a REST API with 68+ endpoints, and a CLI tool. This means AI tools like Claude Code, Cursor, and Windsurf can directly create tasks, update progress, and access project context without any copy-paste.',
  },
  {
    question: 'How does the MCP Server work with Claude Code?',
    answer: 'The MCP Server exposes 27+ tools that Claude Code can use directly. After adding our MCP server to your claude_desktop_config.json, Claude can create tasks, update task status, log time, search your knowledge base, and more. It\'s like giving Claude direct access to your project management system.',
  },
  {
    question: 'What is the Knowledge Base and why do I need it?',
    answer: 'The Knowledge Base is persistent AI memory. AI assistants typically forget everything between sessions. Our Knowledge Base lets you store project context, architectural decisions, coding conventions, and other important information that AI can access in future conversations. Think of it as giving your AI a long-term memory.',
  },
  {
    question: 'Is my data secure?',
    answer: 'Yes. We use end-to-end encryption for all data in transit (TLS 1.3), encryption at rest (AES-256), and follow SOC 2 compliance practices. Your data is hosted in secure EU data centers. We never use your data to train AI models. See our Security page for full details.',
  },
  {
    question: 'Can I use Yet.Project with my existing tools?',
    answer: 'Absolutely. Our REST API integrates with any tool that can make HTTP requests. We have native integrations with GitHub, and our MCP server works with any MCP-compatible AI tool. The CLI works in any terminal, and we\'re adding more integrations regularly.',
  },
  {
    question: 'What happens if I exceed the free tier limits?',
    answer: 'The free tier includes 3 projects and 100 tasks. If you need more, you can upgrade to Pro at any time. We\'ll notify you when you\'re approaching limits. Your data is never deleted - you just can\'t create new items until you upgrade or delete existing ones.',
  },
  {
    question: 'Do you offer a free trial for Pro features?',
    answer: 'Yes! When you sign up for Pro, you get a 14-day free trial with full access to all features including MCP Server, Knowledge Base, and unlimited projects. No credit card required to start the trial.',
  },
  {
    question: 'Can AI agents have their own API keys?',
    answer: 'Yes, and this is a key feature. You can create separate API keys for each AI agent with granular permissions (read-only, write, admin). This gives you full visibility into what each agent is doing and the ability to revoke access if needed.',
  },
];
---

<Layout
  title="AI-Native Project Management for Developers"
  description="Let Claude Code, Cursor, and AI tools manage your projects directly. The only project management platform built for human + AI collaboration with MCP, API, and CLI."
>
  <Header />

  <main id="main-content">
    <!-- Hero Section -->
    <section class="relative min-h-screen flex items-center justify-center overflow-hidden pt-20 lg:pt-0">
      <!-- Background gradient mesh -->
      <div class="absolute inset-0 bg-gradient-mesh opacity-70"></div>

      <!-- Grid pattern overlay -->
      <div class="absolute inset-0 bg-dots-pattern opacity-30"></div>

      <!-- Decorative orbs -->
      <div class="absolute top-1/4 -left-32 w-96 h-96 bg-primary-400/20 rounded-full blur-3xl animate-float"></div>
      <div class="absolute bottom-1/4 -right-32 w-96 h-96 bg-secondary-400/20 rounded-full blur-3xl animate-float" style="animation-delay: -3s;"></div>
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-gradient-radial from-primary-500/5 to-transparent rounded-full"></div>

      <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 lg:py-32">
        <div class="text-center">
          <!-- Badge -->
          <div class="inline-flex items-center gap-2 bg-white/80 backdrop-blur-sm border border-dark-200/50 shadow-lg shadow-dark-900/5 px-4 py-2 rounded-full text-sm font-medium mb-8" data-animate>
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent-400 opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-accent-500"></span>
            </span>
            <span class="text-dark-600">Now with <span class="text-primary-600 font-semibold">MCP Server</span> & <span class="text-secondary-600 font-semibold">Knowledge Base</span></span>
          </div>

          <!-- Headline -->
          <h1 class="text-4xl sm:text-5xl lg:text-display-xl font-bold text-dark-900 mb-6 tracking-tight" data-animate data-delay="100">
            Project Management<br />
            <span class="gradient-text">for AI Agents</span>
          </h1>

          <!-- Subheadline -->
          <p class="text-lg sm:text-xl text-dark-600 mb-10 max-w-2xl mx-auto leading-relaxed" data-animate data-delay="200">
            Let Claude Code, Cursor, and your AI tools manage projects directly.
            The only PM platform built for <span class="font-semibold text-dark-800">human + AI collaboration</span>.
          </p>

          <!-- CTAs -->
          <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-12" data-animate data-delay="300">
            <a
              href="https://app.yet.watch/auth/register"
              class="group relative w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-gradient-to-r from-primary-600 to-primary-500 text-white px-8 py-4 rounded-xl font-semibold text-lg shadow-xl shadow-primary-500/25 hover:shadow-primary-500/40 hover:from-primary-500 hover:to-primary-600 transition-all duration-300 btn-glow"
              data-track-cta="start_free"
              data-track-location="hero"
            >
              <span>Start Free</span>
              <svg class="w-5 h-5 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
            <a
              href="#how-it-works"
              class="group w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-white/80 backdrop-blur-sm border border-dark-200 text-dark-700 px-8 py-4 rounded-xl font-semibold text-lg hover:bg-white hover:border-dark-300 hover:shadow-lg transition-all duration-300"
              data-track-cta="see_how_it_works"
              data-track-location="hero"
            >
              <svg class="w-5 h-5 text-primary-500" fill="currentColor" viewBox="0 0 24 24">
                <path d="M8 5v14l11-7z"/>
              </svg>
              <span>See How It Works</span>
            </a>
          </div>

          <!-- Trust signal -->
          <p class="text-sm text-dark-500 mb-12" data-animate data-delay="400">
            No credit card required &middot; Set up in 2 minutes
          </p>

          <!-- Terminal Demo -->
          <div class="max-w-3xl mx-auto" data-animate="scale" data-delay="500">
            <div class="relative bg-dark-900 rounded-2xl shadow-2xl shadow-dark-900/50 overflow-hidden border border-dark-700/50">
              <!-- Terminal header -->
              <div class="flex items-center gap-2 px-4 py-3 bg-dark-800/50 border-b border-dark-700/50">
                <div class="flex gap-1.5">
                  <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
                  <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
                  <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
                </div>
                <span class="text-dark-400 text-xs font-mono ml-2">zsh — yet-project</span>
              </div>

              <!-- Terminal content -->
              <div class="p-6 font-mono text-sm text-left overflow-x-auto">
                <div class="space-y-3">
                  <div>
                    <span class="text-dark-500"># Ask Claude to create a task</span>
                  </div>
                  <div class="flex items-start gap-2">
                    <span class="text-accent-400">claude</span>
                    <span class="text-dark-300">"Create a high-priority task for implementing OAuth"</span>
                  </div>
                  <div class="pl-4 border-l-2 border-dark-700 space-y-1 mt-3">
                    <div class="text-primary-400">Using yet.create_task tool...</div>
                    <div class="text-dark-400">
                      <span class="text-accent-400">✓</span> Created task in project "Backend API"
                    </div>
                    <div class="text-dark-500">
                      <span class="text-dark-600">ID:</span> task_7kd9f2
                      <span class="text-dark-600 ml-4">Priority:</span> <span class="text-red-400">High</span>
                      <span class="text-dark-600 ml-4">Status:</span> <span class="text-yellow-400">To Do</span>
                    </div>
                  </div>
                  <div class="mt-4">
                    <span class="text-dark-500"># Claude can update progress as it works</span>
                  </div>
                  <div class="pl-4 border-l-2 border-dark-700 space-y-1">
                    <div class="text-primary-400">Using yet.update_task tool...</div>
                    <div class="text-dark-400">
                      <span class="text-accent-400">✓</span> Updated task_7kd9f2
                    </div>
                    <div class="text-dark-500">
                      <span class="text-dark-600">Progress:</span> <span class="text-accent-400">75%</span>
                      <span class="text-dark-600 ml-4">Status:</span> <span class="text-blue-400">In Progress</span>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Glow effect -->
              <div class="absolute -bottom-20 left-1/2 -translate-x-1/2 w-2/3 h-40 bg-primary-500/20 blur-3xl"></div>
            </div>
          </div>
        </div>
      </div>

      <!-- Scroll indicator -->
      <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce-subtle">
        <svg class="w-6 h-6 text-dark-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
        </svg>
      </div>
    </section>

    <!-- Stats Section -->
    <section class="relative py-16 bg-dark-900 border-y border-dark-800">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-8">
          {stats.map((stat, index) => (
            <div class="text-center" data-animate data-delay={String(index * 100)}>
              <div class="text-4xl lg:text-5xl font-bold text-white mb-2">{stat.value}</div>
              <div class="text-dark-400 font-medium">{stat.label}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Problem/Solution Section -->
    <section class="py-24 lg:py-32 bg-dark-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Your AI Can Write Code.<br />
            <span class="gradient-text">But Can It Manage Projects?</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            AI assistants are powerful, but they forget everything between sessions and can't interact with your project management tools.
          </p>
        </div>

        <div class="grid lg:grid-cols-2 gap-8 lg:gap-12">
          <!-- Problems -->
          <div class="bg-white rounded-2xl p-8 shadow-card border border-dark-100" data-animate data-delay="100">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 rounded-xl bg-red-100 flex items-center justify-center">
                <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-dark-900">Without Yet.Project</h3>
            </div>
            <ul class="space-y-4">
              {[
                'AI assistants forget context between sessions',
                'No visibility into what AI changed vs humans',
                'Existing PM tools weren\'t built for AI workflows',
                'Copy-pasting between chat and task boards',
                'No programmatic access for automation',
              ].map(problem => (
                <li class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-red-500 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span class="text-dark-600">{problem}</span>
                </li>
              ))}
            </ul>
          </div>

          <!-- Solutions -->
          <div class="bg-gradient-to-br from-primary-600 to-primary-700 rounded-2xl p-8 shadow-xl" data-animate data-delay="200">
            <div class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 rounded-xl bg-white/20 flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-white">With Yet.Project</h3>
            </div>
            <ul class="space-y-4">
              {[
                'Knowledge Base = persistent AI memory',
                'Activity tracking shows AI vs human actions',
                'MCP Server gives AI direct task access',
                'CLI + API = no copy-paste needed',
                'Built for developers, not adapted for them',
              ].map(solution => (
                <li class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-accent-300 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                  <span class="text-white/90">{solution}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="py-24 lg:py-32">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">Features</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Everything You Need for<br />
            <span class="gradient-text">AI-Powered Development</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            A complete project management platform designed for developers and their AI assistants.
          </p>
        </div>

        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
          {features.map((feature, index) => (
            <div
              class="group relative bg-white rounded-2xl p-8 shadow-card hover:shadow-card-hover border border-dark-100 hover:border-dark-200 transition-all duration-300"
              data-animate
              data-delay={String((index % 3) * 100)}
            >
              <!-- Icon -->
              <div class={`w-14 h-14 rounded-2xl mb-6 flex items-center justify-center ${
                feature.color === 'primary' ? 'bg-primary-100 text-primary-600' :
                feature.color === 'secondary' ? 'bg-secondary-100 text-secondary-600' :
                'bg-accent-100 text-accent-600'
              }`}>
                {feature.icon === 'mcp' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M13.19 8.688a4.5 4.5 0 011.242 7.244l-4.5 4.5a4.5 4.5 0 01-6.364-6.364l1.757-1.757m13.35-.622l1.757-1.757a4.5 4.5 0 00-6.364-6.364l-4.5 4.5a4.5 4.5 0 001.242 7.244" />
                  </svg>
                )}
                {feature.icon === 'api' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M17.25 6.75L22.5 12l-5.25 5.25m-10.5 0L1.5 12l5.25-5.25m7.5-3l-4.5 16.5" />
                  </svg>
                )}
                {feature.icon === 'cli' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 7.5l3 2.25-3 2.25m4.5 0h3m-9 8.25h13.5A2.25 2.25 0 0021 18V6a2.25 2.25 0 00-2.25-2.25H5.25A2.25 2.25 0 003 6v12a2.25 2.25 0 002.25 2.25z" />
                  </svg>
                )}
                {feature.icon === 'knowledge' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 18v-5.25m0 0a6.01 6.01 0 001.5-.189m-1.5.189a6.01 6.01 0 01-1.5-.189m3.75 7.478a12.06 12.06 0 01-4.5 0m3.75 2.383a14.406 14.406 0 01-3 0M14.25 18v-.192c0-.983.658-1.823 1.508-2.316a7.5 7.5 0 10-7.517 0c.85.493 1.509 1.333 1.509 2.316V18" />
                  </svg>
                )}
                {feature.icon === 'keys' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 5.25a3 3 0 013 3m3 0a6 6 0 01-7.029 5.912c-.563-.097-1.159.026-1.563.43L10.5 17.25H8.25v2.25H6v2.25H2.25v-2.818c0-.597.237-1.17.659-1.591l6.499-6.499c.404-.404.527-1 .43-1.563A6 6 0 1121.75 8.25z" />
                  </svg>
                )}
                {feature.icon === 'activity' && (
                  <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3 13.125C3 12.504 3.504 12 4.125 12h2.25c.621 0 1.125.504 1.125 1.125v6.75C7.5 20.496 6.996 21 6.375 21h-2.25A1.125 1.125 0 013 19.875v-6.75zM9.75 8.625c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125v11.25c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V8.625zM16.5 4.125c0-.621.504-1.125 1.125-1.125h2.25C20.496 3 21 3.504 21 4.125v15.75c0 .621-.504 1.125-1.125 1.125h-2.25a1.125 1.125 0 01-1.125-1.125V4.125z" />
                  </svg>
                )}
              </div>

              <h3 class="text-xl font-semibold text-dark-900 mb-3 group-hover:text-primary-600 transition-colors">
                {feature.title}
              </h3>

              <p class="text-dark-600 leading-relaxed">
                {feature.description}
              </p>

              <!-- Hover arrow -->
              <div class="absolute bottom-8 right-8 opacity-0 group-hover:opacity-100 transition-opacity">
                <svg class="w-5 h-5 text-primary-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Integrations Section -->
    <section id="integrations" class="py-24 lg:py-32 bg-dark-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">Integrations</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Works With Your Favorite<br />
            <span class="gradient-text">AI Tools & IDEs</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            Native MCP support for Claude, Cursor, and more. Full API access for any custom integration.
          </p>
        </div>

        <!-- Integration logos -->
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4" data-animate data-delay="100">
          {integrations.map((integration, index) => (
            <div
              class="group bg-white rounded-xl p-6 shadow-card hover:shadow-card-hover border border-dark-100 hover:border-primary-200 transition-all duration-300 flex flex-col items-center justify-center gap-3"
              data-delay={String(index * 50)}
            >
              <!-- Placeholder logo -->
              <div class="w-12 h-12 rounded-xl bg-dark-100 group-hover:bg-primary-100 transition-colors flex items-center justify-center">
                <svg class="w-6 h-6 text-dark-400 group-hover:text-primary-600 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.19 8.688a4.5 4.5 0 011.242 7.244l-4.5 4.5a4.5 4.5 0 01-6.364-6.364l1.757-1.757m13.35-.622l1.757-1.757a4.5 4.5 0 00-6.364-6.364l-4.5 4.5a4.5 4.5 0 001.242 7.244" />
                </svg>
              </div>
              <span class="text-sm font-medium text-dark-700 group-hover:text-primary-600 transition-colors">{integration.name}</span>
            </div>
          ))}
        </div>

        <!-- MCP Config Example -->
        <div class="mt-16 max-w-3xl mx-auto" data-animate data-delay="200">
          <div class="bg-dark-900 rounded-2xl shadow-2xl overflow-hidden border border-dark-700/50">
            <div class="flex items-center gap-2 px-4 py-3 bg-dark-800/50 border-b border-dark-700/50">
              <span class="text-dark-400 text-xs font-mono">claude_desktop_config.json</span>
            </div>
            <pre class="p-6 overflow-x-auto text-sm"><code class="font-mono"><span class="text-dark-500">&#123;</span>
  <span class="text-primary-400">"mcpServers"</span><span class="text-dark-500">:</span> <span class="text-dark-500">&#123;</span>
    <span class="text-primary-400">"yet"</span><span class="text-dark-500">:</span> <span class="text-dark-500">&#123;</span>
      <span class="text-secondary-400">"command"</span><span class="text-dark-500">:</span> <span class="text-accent-400">"npx"</span><span class="text-dark-500">,</span>
      <span class="text-secondary-400">"args"</span><span class="text-dark-500">:</span> <span class="text-dark-500">[</span><span class="text-accent-400">"-y"</span><span class="text-dark-500">,</span> <span class="text-accent-400">"@anthropic/yet-mcp"</span><span class="text-dark-500">],</span>
      <span class="text-secondary-400">"env"</span><span class="text-dark-500">:</span> <span class="text-dark-500">&#123;</span>
        <span class="text-secondary-400">"YET_API_KEY"</span><span class="text-dark-500">:</span> <span class="text-accent-400">"yet_sk_..."</span>
      <span class="text-dark-500">&#125;</span>
    <span class="text-dark-500">&#125;</span>
  <span class="text-dark-500">&#125;</span>
<span class="text-dark-500">&#125;</span></code></pre>
          </div>
        </div>
      </div>
    </section>

    <!-- How It Works Section -->
    <section id="how-it-works" class="py-24 lg:py-32">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">How It Works</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Get Started in<br />
            <span class="gradient-text">Three Simple Steps</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            Set up AI-powered project management in minutes, not hours.
          </p>
        </div>

        <div class="space-y-12 lg:space-y-16">
          {steps.map((step, index) => (
            <div
              class={`flex flex-col ${index % 2 === 1 ? 'lg:flex-row-reverse' : 'lg:flex-row'} gap-8 lg:gap-16 items-center`}
              data-animate
              data-delay={String(index * 100)}
            >
              <!-- Content -->
              <div class="flex-1 text-center lg:text-left">
                <div class="inline-flex items-center justify-center w-12 h-12 rounded-full bg-primary-100 text-primary-600 font-bold text-lg mb-4">
                  {step.step}
                </div>
                <h3 class="text-2xl font-bold text-dark-900 mb-3">{step.title}</h3>
                <p class="text-dark-600 text-lg leading-relaxed">{step.description}</p>
              </div>

              <!-- Code block -->
              <div class="flex-1 w-full">
                <div class="bg-dark-900 rounded-xl shadow-xl overflow-hidden border border-dark-700/50">
                  <div class="flex items-center gap-1.5 px-4 py-2.5 bg-dark-800/50 border-b border-dark-700/50">
                    <div class="w-2.5 h-2.5 rounded-full bg-red-500/80"></div>
                    <div class="w-2.5 h-2.5 rounded-full bg-yellow-500/80"></div>
                    <div class="w-2.5 h-2.5 rounded-full bg-green-500/80"></div>
                  </div>
                  <pre class="p-4 overflow-x-auto text-sm"><code class="font-mono text-dark-300 whitespace-pre">{step.code}</code></pre>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Video Demo Section -->
    <section id="demo" class="py-24 lg:py-32 bg-gradient-to-b from-white to-dark-50">
      <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">See It In Action</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Watch How AI<br />
            <span class="gradient-text">Manages Your Projects</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            See Claude Code create tasks, update progress, and access project context in real-time.
          </p>
        </div>

        <!-- Video Player -->
        <div class="relative rounded-2xl overflow-hidden shadow-2xl shadow-dark-900/20" data-animate data-delay="100">
          <!-- Video Thumbnail / Placeholder -->
          <div class="relative aspect-video bg-dark-900 group cursor-pointer" id="video-container">
            <!-- Thumbnail Image -->
            <img
              src="/demo-thumbnail.jpg"
              alt="Yet.Project Demo Video"
              class="absolute inset-0 w-full h-full object-cover opacity-80 group-hover:opacity-100 transition-opacity"
              onerror="this.style.display='none'"
            />

            <!-- Gradient Overlay -->
            <div class="absolute inset-0 bg-gradient-to-t from-dark-900/80 via-dark-900/20 to-transparent"></div>

            <!-- Play Button -->
            <button
              id="play-demo-btn"
              class="absolute inset-0 flex items-center justify-center group/play"
              aria-label="Play demo video"
              data-track-cta="play_demo_video"
              data-track-location="demo_section"
            >
              <div class="relative">
                <div class="w-20 h-20 lg:w-24 lg:h-24 rounded-full bg-white/90 backdrop-blur-sm flex items-center justify-center shadow-xl group-hover/play:scale-110 transition-transform duration-300">
                  <svg class="w-8 h-8 lg:w-10 lg:h-10 text-primary-600 ml-1" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </div>
                <div class="absolute inset-0 rounded-full bg-white/30 animate-ping"></div>
              </div>
            </button>

            <!-- Duration Badge -->
            <div class="absolute bottom-4 right-4 bg-dark-900/80 backdrop-blur-sm text-white text-sm font-medium px-3 py-1 rounded-lg">
              2:34
            </div>

            <!-- Caption -->
            <div class="absolute bottom-4 left-4 text-white">
              <p class="font-semibold">Quick Demo: AI-Powered Project Management</p>
              <p class="text-sm text-dark-300">See the MCP Server, CLI, and API in action</p>
            </div>
          </div>

          <!-- Video iframe (hidden until play) -->
          <div id="video-player" class="hidden aspect-video">
            <!-- YouTube or Vimeo embed will go here -->
          </div>
        </div>

        <!-- Feature highlights below video -->
        <div class="grid grid-cols-3 gap-4 mt-8" data-animate data-delay="200">
          <div class="text-center p-4">
            <div class="text-2xl font-bold text-dark-900">2 min</div>
            <div class="text-sm text-dark-500">Setup Time</div>
          </div>
          <div class="text-center p-4 border-x border-dark-200">
            <div class="text-2xl font-bold text-dark-900">Zero</div>
            <div class="text-sm text-dark-500">Copy-Paste</div>
          </div>
          <div class="text-center p-4">
            <div class="text-2xl font-bold text-dark-900">Real-time</div>
            <div class="text-sm text-dark-500">Sync</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Pricing Section -->
    <section id="pricing" class="py-24 lg:py-32 bg-dark-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">Pricing</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Simple, Transparent<br />
            <span class="gradient-text">Pricing for Everyone</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            Start free, upgrade when you need more. No hidden fees, no surprises.
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
          {pricing.map((plan, index) => (
            <div
              class={`relative rounded-2xl p-8 ${
                plan.highlighted
                  ? 'bg-gradient-to-br from-primary-600 to-primary-700 text-white shadow-2xl shadow-primary-500/30 ring-4 ring-primary-400/20 scale-105 z-10'
                  : 'bg-white shadow-card border border-dark-100'
              }`}
              data-animate
              data-delay={String(index * 100)}
            >
              {plan.highlighted && (
                <div class="absolute -top-4 left-1/2 -translate-x-1/2 bg-accent-500 text-white text-xs font-semibold px-3 py-1 rounded-full">
                  Most Popular
                </div>
              )}

              <h3 class={`text-xl font-bold mb-2 ${plan.highlighted ? 'text-white' : 'text-dark-900'}`}>
                {plan.name}
              </h3>

              <div class="mb-4">
                <span class={`text-4xl font-bold ${plan.highlighted ? 'text-white' : 'text-dark-900'}`}>
                  {plan.price}
                </span>
                <span class={plan.highlighted ? 'text-primary-100' : 'text-dark-500'}>
                  {plan.period}
                </span>
              </div>

              <p class={`mb-6 ${plan.highlighted ? 'text-primary-100' : 'text-dark-600'}`}>
                {plan.description}
              </p>

              <ul class="space-y-3 mb-8">
                {plan.features.map(feature => (
                  <li class="flex items-center gap-3">
                    <svg class={`w-5 h-5 flex-shrink-0 ${plan.highlighted ? 'text-accent-300' : 'text-accent-500'}`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    <span class={plan.highlighted ? 'text-white/90' : 'text-dark-600'}>
                      {feature}
                    </span>
                  </li>
                ))}
              </ul>

              <a
                href={plan.href}
                class={`block text-center py-3 px-6 rounded-xl font-semibold transition-all duration-300 ${
                  plan.highlighted
                    ? 'bg-white text-primary-600 hover:bg-dark-50 shadow-lg'
                    : 'bg-dark-100 text-dark-700 hover:bg-dark-200'
                }`}
                data-track-cta={`pricing_${plan.name.toLowerCase()}`}
                data-track-location="pricing"
              >
                {plan.cta}
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section id="faq" class="py-24 lg:py-32">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-animate>
          <span class="inline-block text-sm font-semibold text-primary-600 tracking-wide uppercase mb-4">FAQ</span>
          <h2 class="text-3xl lg:text-display-md font-bold text-dark-900 mb-4">
            Frequently Asked<br />
            <span class="gradient-text">Questions</span>
          </h2>
          <p class="text-lg text-dark-600 max-w-2xl mx-auto">
            Everything you need to know about Yet.Project and AI-native project management.
          </p>
        </div>

        <div class="space-y-4" data-animate data-delay="100">
          {faqs.map((faq, index) => (
            <div class="faq-item border border-dark-200 rounded-xl overflow-hidden bg-white hover:border-primary-300 transition-colors">
              <button
                class="faq-trigger w-full flex items-center justify-between gap-4 p-6 text-left"
                aria-expanded="false"
                data-track-cta={`faq_${index + 1}`}
                data-track-location="faq"
              >
                <span class="font-semibold text-dark-900 pr-4">{faq.question}</span>
                <svg class="faq-icon w-5 h-5 text-dark-400 flex-shrink-0 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </button>
              <div class="faq-content hidden">
                <div class="px-6 pb-6 text-dark-600 leading-relaxed">
                  {faq.answer}
                </div>
              </div>
            </div>
          ))}
        </div>

        <!-- More questions CTA -->
        <div class="text-center mt-12" data-animate data-delay="200">
          <p class="text-dark-600 mb-4">Still have questions?</p>
          <a
            href="/contact"
            class="inline-flex items-center gap-2 text-primary-600 font-semibold hover:text-primary-700 transition-colors"
            data-track-cta="contact_from_faq"
            data-track-location="faq"
          >
            Contact our team
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- Final CTA Section -->
    <section class="relative py-24 lg:py-32 bg-dark-900 overflow-hidden">
      <!-- Background effects -->
      <div class="absolute inset-0 bg-gradient-mesh opacity-20"></div>
      <div class="absolute top-0 left-1/4 w-96 h-96 bg-primary-500/20 rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-secondary-500/20 rounded-full blur-3xl"></div>

      <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl lg:text-display-md font-bold text-white mb-6" data-animate>
          Ready to Let AI<br />
          <span class="bg-gradient-to-r from-primary-400 to-secondary-400 bg-clip-text text-transparent">Manage Your Projects?</span>
        </h2>

        <p class="text-xl text-dark-300 mb-10 max-w-2xl mx-auto" data-animate data-delay="100">
          Join thousands of developers using Yet.Project with Claude Code, Cursor, and other AI tools.
        </p>

        <div class="flex flex-col sm:flex-row items-center justify-center gap-4" data-animate data-delay="200">
          <a
            href="https://app.yet.watch/auth/register"
            class="group w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-white text-dark-900 px-8 py-4 rounded-xl font-semibold text-lg hover:bg-dark-100 transition-all duration-300 shadow-xl"
            data-track-cta="get_started_free"
            data-track-location="final_cta"
          >
            <span>Get Started Free</span>
            <svg class="w-5 h-5 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
          <a
            href="/contact"
            class="w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-dark-800 text-white border border-dark-600 px-8 py-4 rounded-xl font-semibold text-lg hover:bg-dark-700 hover:border-dark-500 transition-all duration-300"
            data-track-cta="talk_to_sales"
            data-track-location="final_cta"
          >
            Talk to Sales
          </a>
        </div>

        <p class="text-sm text-dark-500 mt-6" data-animate data-delay="300">
          No credit card required &middot; 14-day free trial on Pro
        </p>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<script>
  // FAQ Accordion
  document.querySelectorAll('.faq-trigger').forEach(trigger => {
    trigger.addEventListener('click', () => {
      const item = trigger.closest('.faq-item');
      const content = item?.querySelector('.faq-content');
      const icon = item?.querySelector('.faq-icon');
      const isOpen = trigger.getAttribute('aria-expanded') === 'true';

      // Close all other items
      document.querySelectorAll('.faq-item').forEach(otherItem => {
        if (otherItem !== item) {
          const otherTrigger = otherItem.querySelector('.faq-trigger');
          const otherContent = otherItem.querySelector('.faq-content');
          const otherIcon = otherItem.querySelector('.faq-icon');
          otherTrigger?.setAttribute('aria-expanded', 'false');
          otherContent?.classList.add('hidden');
          otherIcon?.classList.remove('rotate-180');
        }
      });

      // Toggle current item
      trigger.setAttribute('aria-expanded', String(!isOpen));
      content?.classList.toggle('hidden');
      icon?.classList.toggle('rotate-180');
    });
  });

  // Video Demo Player
  const playBtn = document.getElementById('play-demo-btn');
  const videoContainer = document.getElementById('video-container');
  const videoPlayer = document.getElementById('video-player');

  playBtn?.addEventListener('click', () => {
    // Replace with your actual video embed URL
    const videoUrl = 'https://www.youtube.com/embed/dQw4w9WgXcQ?autoplay=1&rel=0';

    if (videoPlayer && videoContainer) {
      videoPlayer.innerHTML = `<iframe src="${videoUrl}" class="w-full h-full" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>`;
      videoContainer.classList.add('hidden');
      videoPlayer.classList.remove('hidden');
    }
  });
</script>
