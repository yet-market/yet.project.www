---
import DocLayout from '../../../layouts/DocLayout.astro';
---

<DocLayout title="CLI Commands" description="Complete reference for all Yet.Project CLI commands.">
  <h1>CLI Commands Reference</h1>

  <p>
    Complete list of all available CLI commands.
  </p>

  <h2>Task Commands</h2>

  <h3>yet task list</h3>
  <p>List tasks in a project.</p>
  <pre><code>{`yet task list [--project PROJECT] [--status STATUS] [--assignee USER]

# Examples
yet task list
yet task list --status todo
yet task list --project MYAPP --status in_progress`}</code></pre>

  <h3>yet task create</h3>
  <p>Create a new task.</p>
  <pre><code>{`yet task create TITLE [--project PROJECT] [--priority PRIORITY] [--label LABEL]

# Examples
yet task create "Implement auth"
yet task create "Fix bug" --priority high --label bug
yet task create "Add tests" --project MYAPP --assignee @me`}</code></pre>

  <h3>yet task show</h3>
  <p>Show task details.</p>
  <pre><code>{`yet task show TASK_ID

# Example
yet task show MYAPP-123`}</code></pre>

  <h3>yet task update</h3>
  <p>Update a task.</p>
  <pre><code>{`yet task update TASK_ID [--status STATUS] [--priority PRIORITY] [--assignee USER]

# Examples
yet task update MYAPP-123 --status done
yet task update MYAPP-123 --priority urgent --assignee john@example.com`}</code></pre>

  <h3>yet task delete</h3>
  <p>Delete a task.</p>
  <pre><code>{`yet task delete TASK_ID [--force]

# Example
yet task delete MYAPP-123`}</code></pre>

  <h2>Project Commands</h2>

  <h3>yet project list</h3>
  <p>List all projects.</p>
  <pre><code>yet project list</code></pre>

  <h3>yet project create</h3>
  <p>Create a new project.</p>
  <pre><code>{`yet project create NAME --key KEY [--description DESC]

# Example
yet project create "My App" --key MYAPP --description "Main product"`}</code></pre>

  <h3>yet project show</h3>
  <p>Show project details and statistics.</p>
  <pre><code>{`yet project show PROJECT_KEY

# Example
yet project show MYAPP`}</code></pre>

  <h2>Knowledge Base Commands</h2>

  <h3>yet kb list</h3>
  <p>List knowledge items.</p>
  <pre><code>{`yet kb list [--prefix PREFIX]

# Examples
yet kb list
yet kb list --prefix api/`}</code></pre>

  <h3>yet kb get</h3>
  <p>Get a knowledge item.</p>
  <pre><code>{`yet kb get KEY

# Example
yet kb get architecture`}</code></pre>

  <h3>yet kb set</h3>
  <p>Create or update a knowledge item.</p>
  <pre><code>{`yet kb set KEY CONTENT

# Examples
yet kb set architecture "Microservices with Node.js..."
yet kb set api/auth "JWT tokens with 24h expiry..."

# From file
yet kb set architecture --file ./docs/architecture.md`}</code></pre>

  <h3>yet kb delete</h3>
  <p>Delete a knowledge item.</p>
  <pre><code>{`yet kb delete KEY

# Example
yet kb delete old-docs`}</code></pre>

  <h3>yet kb search</h3>
  <p>Search the knowledge base.</p>
  <pre><code>{`yet kb search QUERY

# Example
yet kb search "authentication"`}</code></pre>

  <h2>Configuration Commands</h2>

  <h3>yet config set</h3>
  <p>Set a configuration value.</p>
  <pre><code>{`yet config set KEY VALUE

# Examples
yet config set defaultProject MYAPP
yet config set format json`}</code></pre>

  <h3>yet config get</h3>
  <p>Get a configuration value.</p>
  <pre><code>yet config get defaultProject</code></pre>

  <h3>yet config list</h3>
  <p>List all configuration.</p>
  <pre><code>yet config list</code></pre>

  <h2>Other Commands</h2>

  <h3>yet login</h3>
  <p>Authenticate with your API key.</p>
  <pre><code>{`yet login [--key KEY]

# Interactive
yet login

# With key
yet login --key yet_xxx`}</code></pre>

  <h3>yet logout</h3>
  <p>Remove stored credentials.</p>
  <pre><code>yet logout</code></pre>

  <h3>yet whoami</h3>
  <p>Show current user and tenant.</p>
  <pre><code>yet whoami</code></pre>

  <h2>Global Options</h2>

  <table>
    <thead>
      <tr>
        <th>Option</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>--help, -h</code></td>
        <td>Show help for command</td>
      </tr>
      <tr>
        <td><code>--version, -v</code></td>
        <td>Show CLI version</td>
      </tr>
      <tr>
        <td><code>--json</code></td>
        <td>Output as JSON</td>
      </tr>
      <tr>
        <td><code>--quiet, -q</code></td>
        <td>Suppress output</td>
      </tr>
    </tbody>
  </table>

  <h2>Next Steps</h2>

  <ul>
    <li><a href="/docs/cli/git">Git Integration</a></li>
    <li><a href="/docs/cli">CLI Installation</a></li>
  </ul>
</DocLayout>
